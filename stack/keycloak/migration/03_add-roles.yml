id: add roles
author: Default
realm: local_realm
changes:
  - addRole:
      name: sbb-sachbearbeiter
      description: Processes applications by reviewing them, ensuring compliance and communicating with applicants for clarification.
      clientRole: true
      clientId: local
      composite: false

  - addRole:
      name: sbb-system
      description: Provides endpoint and interface access restricted to system accounts.
      clientRole: true
      clientId: local
      composite: false
